@model KPIMonitor.Models.DimObjective
@{
    ViewData["Title"] = "Set Objective Active";
}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow rounded-4 border-success">
                <div class="card-header bg-white border-bottom">
                    <h4 class="mb-0 text-dark fw-bold">✅ Set Objective Active</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success rounded-4">
                        Set objective
                        <strong>@Model.ObjectiveName</strong> (Code: <strong>@Model.ObjectiveCode</strong>)
                        under pillar <strong>@($"{Model.Pillar?.PillarCode} — {Model.Pillar?.PillarName}")</strong> to <strong>Active</strong>?
                    </div>

                    <form asp-action="Activate" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@Model.ObjectiveId" />
                        <div asp-validation-summary="All" class="alert alert-danger rounded-4"></div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Who is making it active? (Last Changed By)</label>
                            <input name="lastChangedBy" value="@Model.LastChangedBy" class="form-control" maxlength="150" />
                            <span class="text-danger">@Html.ValidationMessage("lastChangedBy")</span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success btn-lg">Confirm Activate</button>
                            <a class="btn btn-outline-secondary btn-lg" href="@Url.Action("Index")">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
